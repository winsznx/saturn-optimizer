## Example testnet settings only.
## Use a local ignored `settings/Testnet.toml` for real RPC endpoints or mnemonics.

[network]
name = "testnet"
stacks_node_rpc_address = "https://api.testnet.hiro.so"
deployment_fee_rate = 10

[accounts.deployer]
mnemonic = "<YOUR PRIVATE TESTNET MNEMONIC HERE>"
